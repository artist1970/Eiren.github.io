# REMOVE FROM PUBLIC REPOSITORY

The current public repository contains:

`eiren-adapter.js`

That file should not remain in the public-facing repository because it contains internal architecture and routing language.

Do not copy it into the redesigned public package.

If its behavior is still needed elsewhere, preserve or rebuild it only in the appropriate private infrastructure location.
